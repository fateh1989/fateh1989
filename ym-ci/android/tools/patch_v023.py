from pathlib import Path

SERVICE = Path("ym-ci/android/app/src/main/java/com/ym/lite/automation/YmTikTokAccessibilityService.kt")
ACTIVITY = Path("ym-ci/android/app/src/main/java/com/ym/lite/TikTokAutoActivity.kt")


def replace_once(text: str, old: str, new: str, label: str) -> str:
    count = text.count(old)
    if count != 1:
        raise SystemExit(f"{label}: expected 1 match, found {count}")
    return text.replace(old, new, 1)


s = SERVICE.read_text()
s = replace_once(s, "import android.view.Gravity\nimport android.view.WindowManager", "import android.view.Gravity\nimport android.view.View\nimport android.view.WindowManager", "view import")
s = replace_once(s, "import android.widget.TextView\nimport android.widget.Toast", "import android.widget.LinearLayout\nimport android.widget.TextView\nimport android.widget.Toast", "linear layout import")
s = replace_once(s, "    private var overlay: TextView? = null\n    private var enabled = false", "    private var overlayContainer: LinearLayout? = null\n    private var autoButton: TextView? = null\n    private var commentButton: TextView? = null\n    private var enabled = false\n    private var commentModeActive = false", "overlay fields")
s = replace_once(s, "        instance = this\n        reloadFromPrefs()", "        instance = this\n        prefs.edit().putBoolean(\"comment_mode_active\", false).apply()\n        reloadFromPrefs()", "service connect reset")
s = replace_once(s, "        removeOverlay()\n        if (instance === this) instance = null", "        removeOverlay()\n        prefs.edit().putBoolean(\"comment_mode_active\", false).apply()\n        if (instance === this) instance = null", "destroy reset")
s = replace_once(s, "        autoComment = prefs.getBoolean(\"auto_comment\", false)\n        smartWatch = prefs.getBoolean(\"smart_watch\", true)", "        autoComment = prefs.getBoolean(\"auto_comment\", false)\n        commentModeActive = prefs.getBoolean(\"comment_mode_active\", false)\n        smartWatch = prefs.getBoolean(\"smart_watch\", true)", "reload comment mode")
s = replace_once(s, "        comments = localPrefs.getString(\"comment_pool\", \"\").orEmpty()\n            .lineSequence().map { it.trim() }.filter { it.isNotEmpty() }.toList()\n", "        comments = localPrefs.getString(\"comment_pool\", \"\").orEmpty()\n            .lineSequence().map { it.trim() }.filter { it.isNotEmpty() }.toList()\n        commentIndex = localPrefs.getInt(\"comment_wheel_index\", 0).coerceAtLeast(0)\n", "persist wheel index")
s = replace_once(s, "            scrollCount = 0\n            commentIndex = 0\n            commentsSent = 0", "            scrollCount = 0\n            commentsSent = 0", "do not reset wheel")
s = replace_once(s, "        autoComment = prefs.getBoolean(\"auto_comment\", false)\n        smartWatch = prefs.getBoolean(\"smart_watch\", true)\n        if (!enabled || !ensureSessionActive()) return", "        autoComment = prefs.getBoolean(\"auto_comment\", false)\n        commentModeActive = prefs.getBoolean(\"comment_mode_active\", false)\n        smartWatch = prefs.getBoolean(\"smart_watch\", true)\n        if (!enabled || !ensureSessionActive()) return", "tick comment mode")
old_advance = """        val now = System.currentTimeMillis()\n        val commentDueByVideo = (scrollCount + 1) % commentEvery == 0\n        val commentDueByTime = now - lastCommentAt >= commentGapMs\n        val withinSessionLimit = commentsSent < maxCommentsPerSession\n        val shouldComment = autoComment && comments.isNotEmpty() && commentDueByVideo &&\n            commentDueByTime && withinSessionLimit\n"""
s = replace_once(s, old_advance, "        val shouldComment = commentModeActive && comments.isNotEmpty()\n", "comment every video while manually on")
insert_point = """    private fun stopAutopilot(showToast: Boolean) {\n"""
manual_toggle = """    private fun toggleCommentsFromOverlay() {\n        if (currentTikTokRoot() == null) return\n\n        val turningOn = !prefs.getBoolean(\"comment_mode_active\", false)\n        if (turningOn && comments.isEmpty()) {\n            Toast.makeText(this, \"دولاب التعليقات فارغ — أضف التعليقات من YM\", Toast.LENGTH_LONG).show()\n            return\n        }\n\n        prefs.edit().putBoolean(\"comment_mode_active\", turningOn).apply()\n        commentModeActive = turningOn\n        updateOverlayText()\n        Toast.makeText(\n            this,\n            if (turningOn) \"التعليقات التلقائية تعمل\" else \"تم إيقاف التعليقات التلقائية\",\n            Toast.LENGTH_SHORT,\n        ).show()\n    }\n\n"""
s = replace_once(s, insert_point, manual_toggle + insert_point, "manual toggle function")
s = replace_once(s, "            .putBoolean(\"pending_launch\", false)\n            .putLong(\"session_end_at\", 0L)", "            .putBoolean(\"pending_launch\", false)\n            .putBoolean(\"comment_mode_active\", false)\n            .putLong(\"session_end_at\", 0L)", "stop clears comments")
s = replace_once(s, "        handler.removeCallbacks(loop)\n        updateOverlayText()", "        commentModeActive = false\n        handler.removeCallbacks(loop)\n        updateOverlayText()", "stop local comment mode")
s = replace_once(s, "    private fun attemptComment(done: () -> Unit) {\n        if (!ensureSessionActive()) return\n        if (commentsSent >= maxCommentsPerSession) { done(); return }\n        if (System.currentTimeMillis() - lastCommentAt < commentGapMs) { done(); return }\n", "    private fun attemptComment(done: () -> Unit) {\n        if (!ensureSessionActive()) { done(); return }\n        if (!prefs.getBoolean(\"comment_mode_active\", false)) { done(); return }\n        if (comments.isEmpty()) { done(); return }\n", "remove comment limits")
s = replace_once(s, "                if (clickNode(send)) {\n                    commentIndex++\n                    commentsSent++\n                    lastCommentAt = System.currentTimeMillis()\n                    updateOverlayText()\n                }", "                if (clickNode(send)) {\n                    commentIndex = (commentIndex + 1) % comments.size\n                    localPrefs.edit().putInt(\"comment_wheel_index\", commentIndex).apply()\n                    commentsSent++\n                    updateOverlayText()\n                }", "persist next comment")
start = s.index("    private fun ensureOverlay() {")
end = s.index("    private fun removeOverlay() {", start)
end2 = s.index("\n    }\n}", end) + len("\n    }")
overlay_code = '''    private fun ensureOverlay() {\n        if (overlayContainer != null) { updateOverlayText(); return }\n        val wm = getSystemService(WINDOW_SERVICE) as WindowManager\n        val density = resources.displayMetrics.density\n\n        fun bubble(strokeColor: Int, click: () -> Unit): TextView = TextView(this).apply {\n            gravity = Gravity.CENTER\n            setTextColor(Color.WHITE)\n            textSize = 12f\n            minWidth = (74 * density).roundToInt()\n            setPadding((10 * density).roundToInt(), (8 * density).roundToInt(), (10 * density).roundToInt(), (8 * density).roundToInt())\n            background = GradientDrawable().apply {\n                cornerRadius = 22 * density\n                setColor(Color.argb(228, 15, 15, 15))\n                setStroke((2 * density).roundToInt(), strokeColor)\n            }\n            setOnClickListener { click() }\n        }\n\n        val container = LinearLayout(this).apply {\n            orientation = LinearLayout.VERTICAL\n            gravity = Gravity.CENTER_HORIZONTAL\n        }\n        val auto = bubble(Color.rgb(37, 244, 238)) { toggleAutopilotFromOverlay() }\n        auto.setOnLongClickListener {\n            if (currentTikTokRoot() == null) return@setOnLongClickListener false\n            startActivity(Intent(this@YmTikTokAccessibilityService, TikTokAutoActivity::class.java).apply {\n                addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)\n            })\n            true\n        }\n        val commentsToggle = bubble(Color.rgb(255, 75, 145)) { toggleCommentsFromOverlay() }\n        container.addView(auto)\n        container.addView(View(this).apply { layoutParams = LinearLayout.LayoutParams(1, (7 * density).roundToInt()) })\n        container.addView(commentsToggle)\n\n        val lp = WindowManager.LayoutParams(\n            WindowManager.LayoutParams.WRAP_CONTENT,\n            WindowManager.LayoutParams.WRAP_CONTENT,\n            WindowManager.LayoutParams.TYPE_ACCESSIBILITY_OVERLAY,\n            WindowManager.LayoutParams.FLAG_NOT_FOCUSABLE or WindowManager.LayoutParams.FLAG_NOT_TOUCH_MODAL,\n            PixelFormat.TRANSLUCENT,\n        ).apply {\n            gravity = Gravity.END or Gravity.CENTER_VERTICAL\n            x = (10 * density).roundToInt()\n        }\n        wm.addView(container, lp)\n        overlayContainer = container\n        autoButton = auto\n        commentButton = commentsToggle\n        updateOverlayText()\n    }\n\n    private fun updateOverlayText() {\n        val running = prefs.getBoolean(\"enabled\", false) && sessionEndAt > 0L\n        val commentsOn = prefs.getBoolean(\"comment_mode_active\", false)\n        autoButton?.text = if (running) {\n            val remainingMs = (sessionEndAt - System.currentTimeMillis()).coerceAtLeast(0L)\n            val remainingMinutes = (remainingMs + 59_999L) / 60_000L\n            \"■ STOP\\n${remainingMinutes}m\\n↕ $scrollCount\"\n        } else {\n            \"▶ AUTO\\nYM\"\n        }\n        commentButton?.text = if (commentsOn) \"💬 ON\\n$commentsSent\" else \"💬 OFF\"\n        commentButton?.alpha = if (commentsOn) 1f else 0.72f\n    }\n\n    private fun removeOverlay() {\n        val view = overlayContainer ?: return\n        runCatching { (getSystemService(WINDOW_SERVICE) as WindowManager).removeView(view) }\n        overlayContainer = null\n        autoButton = null\n        commentButton = null\n    }'''
s = s[:start] + overlay_code + s[end2:]
SERVICE.write_text(s)

# Simplify the settings screen: comments are started/stopped only from the floating TikTok button.
a = ACTIVITY.read_text()
a = replace_once(a, '        body.addView(autoComment, matchWrap())\n', '', 'hide old auto-comment switch')
a = replace_once(a, '        body.addView(label("ضع تعليقًا كل كم فيديو؟"))\n        commentEvery = numberField("3")\n        body.addView(commentEvery, matchWrap())\n\n        body.addView(label("أقصى عدد تعليقات في جلسة التشغيل — من 1 إلى 50"))\n        maxCommentsSession = numberField("5")\n        body.addView(maxCommentsSession, matchWrap())\n\n        body.addView(label("أقل وقت بين تعليق وآخر — من 30 إلى 3600 ثانية"))\n        commentGapSeconds = numberField("60")\n        body.addView(commentGapSeconds, matchWrap())\n\n', '        body.addView(label("التعليقات: شغّل أو أوقف 💬 يدويًا من الزر العائم داخل TikTok. عند التشغيل يعلّق YM على كل فيديو."))\n\n', 'remove old rate controls')
a = replace_once(a, '.putBoolean("auto_comment", autoComment.isChecked)', '.putBoolean("auto_comment", false)', 'disable legacy auto comment')
a = replace_once(a, '            append("\\nدولاب الدعم: ")\n            append(if (commentState) "مفعّل ($count تعليق)" else "متوقف")\n            if (commentState) append(" — حد الجلسة $maxComments — فاصل ${gap}ث")', '            append("\\nدولاب الدعم: $count تعليق — التشغيل يدوي من زر 💬 داخل TikTok")', 'status text')
ACTIVITY.write_text(a)

print("YM v0.23 patch applied")
