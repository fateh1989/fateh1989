# Cross-session agent instructions

Before working on any existing project or infrastructure for this account:

1. Read `CONTEXT.md`.
2. Resolve the target in `PROJECTS.md`.
3. Read `WORKING_RULES.md`.
4. If the task involves Android/Termux connectivity or device execution, read `BRIDGE.md`.
5. If the task involves heavy-equipment parts/reverse lookup, read `RESEARCH.md` and then verify the web live.
6. Inspect the actual current repository/device/CI state before making changes or claiming success.
7. Never restart a known project from scratch merely because the current conversation lacks prior chat history.
8. Update the canonical handoff files when a durable project mapping, status, architecture, or infrastructure decision changes.

Key mapping: RUN is stored in `fateh1989/1122` on `main`; do not search for a separate RUN repository.
